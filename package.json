{
  "name": "xbreak-monitor",
  "type": "module",
  "scripts": {
    "dev": "op run --env-file=.env.tpl -- bun run --hot src/index.ts",
    "start": "op run --env-file=.env.tpl -- bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "grammy": "^1.39.3",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
